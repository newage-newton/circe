<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.prince.circe.dao.mybatis.JokeMapper">
    <select id="getJokeById" resultType="String" parameterType="Integer">
        select joke
        from jokes.jokes
        where id = #{id}
    </select>

    <insert id="insertJoke" parameterType="String">
        insert into jokes.jokes(joke)
        values (#{joke})
    </insert>

    <select id="getAllJokes" resultType="String">
        select joke
        from jokes.jokes
    </select>
</mapper>